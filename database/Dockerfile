FROM postgres:15-alpine

# Copy initialization scripts
COPY schema/*.sql /docker-entrypoint-initdb.d/

# These scripts run in alphabetical order when container is first created
# 01_init.sql - creates schema
# 02_seed_companies.sql - seeds S&P 500 data (generated by script)
